import React from 'react';
import ViewEntry from  '../components/entries/singleEntry';

const Entry = (props) => (
    <React.Fragment>
      <ViewEntry />
    </React.Fragment>
);

export default Entry;

