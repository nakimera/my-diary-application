import React from 'react';
import Register from '../components/authentication/signUp';

const SignUp = (props) => (
    <React.Fragment>
      <Register {...props}/>
    </React.Fragment>
);

export default SignUp;
